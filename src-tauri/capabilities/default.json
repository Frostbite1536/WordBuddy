{
  "identifier": "default",
  "description": "Capabilities for the main WorkBuddy window. Cursor overlay has its own narrower capability file (cursor-overlay.json).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "global-shortcut:default",
    "sql:default",
    "process:default",
    "dialog:default",
    "log:default",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:window:allow-start-dragging"
  ]
}
